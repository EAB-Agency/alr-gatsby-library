import React from 'react';
import DefaultTiles from './default-tiles'
import './school-tiles-styles.scss';

const SchoolTiles = () => {
  return (
      <DefaultTiles />
  );
};

export default SchoolTiles;
